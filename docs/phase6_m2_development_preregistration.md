# Phase 6 M2 Development Preregistration (Frozen)

本文件冻结 M2 供应中断开发轨道。科学矩阵冻结不等于自动执行：实际运行仍须通过批准指纹、干净Git来源和显式CLI授权双重门槛。

| 维度 | 冻结值 |
|---|---|
| 档位 | V1（1物资、6期、50训练场景） |
| 开发种子 | 2026081201、2026081202、2026081203（与M0/M1全部既有种子隔离） |
| 预算系数 | 0.9、1.1、1.3 |
| 中断档位 | C0、C1、C2 |
| 配置数 | 27 |

C0 显式关闭中断并强制 `alpha=1`；C1/C2 与 C0 共享需求、应急价格、应急供应及潜变量，不引入额外随机抽取。每组须保存实际履约率统计，而不能用名义档位替代。

projection必须从储备区间原始数值独立复算激活，不能信任保存的布尔值。只有C1/C2可作为供应中断激活候选；同一beta的C0必须3/3最优且不能达到2/3实质激活，否则归因为基线混杂并停止。C0/C1/C2还必须通过潜变量、需求、应急价格、应急供应的共同随机数集合哈希核验（履约率允许不同）。

通过规则为合格组合3/3种子最优、端点及固定策略证据完整，且至少2/3满足`R_disc_robust/B >= 0.01`。数值阈值 (`>10^{-4}`) 仅作辅助报告。若没有组合通过，停止该扩展，不继续降低履约率、调整价格或追逐参数。

本执行器 PR 的执行数：开发配置0、pilot 0、正式运行0、M0 E3运行0。正式扩展实验仍未授权。
