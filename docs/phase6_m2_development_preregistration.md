# Phase 6 M2 Development Preregistration (Design Only)

本文件预注册 M2 供应中断开发轨道，但不授权执行。

| 维度 | 冻结候选值 |
|---|---|
| 档位 | V1（1物资、6期、50训练场景） |
| 开发种子 | 2026081201、2026081202、2026081203（与M0/M1全部既有种子隔离） |
| 预算系数 | 0.9、1.1、1.3 |
| 中断档位 | C0、C1、C2 |
| 配置数 | 27 |

C0 显式关闭中断并强制 `alpha=1`；C1/C2 与 C0 共享需求、应急价格、应急供应及潜变量，不引入额外随机抽取。每组须保存实际履约率统计，而不能用名义档位替代。

通过规则为3/3种子最优且至少2/3满足稳健最小容差最优储备比例不低于1%。数值阈值 (10^{-4}) 仅作辅助报告。若没有组合通过，停止该扩展，不继续降低履约率、调整价格或追逐参数。

本设计 PR 的执行数：开发配置0、pilot 0、正式运行0、M0 E3运行0。
